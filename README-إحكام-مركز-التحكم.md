# تعديل إحكام مركز التحكم

استبدل الملف التالي فقط داخل المشروع:

```text
css/style.css
```

ثم افتح `admin.html` واضغط `Ctrl + F5`.

هذا التعديل يجعل سيكشن مركز التحكم ثابتًا أثناء التمرير، لكن داخل حدوده فقط، بخلفية مصمتة وحدود مغلقة، ويمنع ظهور خطوط بطاقات التحرير من خلفه أو تجاوزها جانبيًا.
